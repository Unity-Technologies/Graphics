
# This file is generated by ant_yaml_generation package, don't edit this file manually.
# Introduce any changes under .yamato/config/*.metafile files and rerun yaml-generation (or check #devs-automation-tooling).

{% metadata_file .yamato/_cache_config.metafile -%}
{% metadata_file .yamato/_latest_editor_versions_2020.3.metafile -%}

---

ShaderGraph_OSX_OpenGLCore_playmode_mono_Linear_2020.3:
    name: ShaderGraph on OSX_OpenGLCore_playmode_mono_Linear on version 2020.3
    agent:
        type: Unity::VM::osx
        image: graphics-foundation/mac-10.15:v0.0.1-672710
        flavor: m1.mac
    variables:
        CACHE_ARGS: '{{cache.flags}}'
        CUSTOM_REVISION: '{{2020_3.changeset.id}}'
        TEST_FILTER: .*
        UPM_REGISTRY: https://artifactory-slo.bf.unity3d.com/artifactory/api/npm/upm-candidates
        UTR_VERSION: "current"
    commands:
      -  command: curl -s https://artifactory.prd.it.unity3d.com/artifactory/unity-tools-local/utr-standalone/utr --output utr
         retries: 2
      -  command: chmod +x utr
      -  command: brew tap --force-auto-update unity/unity git@github.cds.internal.unity3d.com:unity/homebrew-unity.git
         retries: 2
      -  command: brew install unity-downloader-cli
         retries: 2
      -  command: unity-downloader-cli --source-file unity_revision.txt -c editor -c il2cpp  --wait --published-only
         retries: 2
      -  command: ./utr  --artifacts_path=TestProjects/ShaderGraph/test-results --editor-location=.Editor --extra-editor-arg="-colorspace=Linear" --scripting-backend=Mono2x --suite=playmode --testfilter=$TEST_FILTER --testproject=./TestProjects/ShaderGraph --zero-tests-are-ok=1
    artifacts:
        logs:
            paths:
              -  "**/test-results/**"
              -  "TestProjects/ShaderGraph/Logs/*.log"
    dependencies:
      -  path: .yamato/_editor_priming.yml#editor:priming:2020.3:OSX
         rerun: on_new_revision
ShaderGraph_OSX_OpenGLCore_editmode_mono_Linear_2020.3:
    name: ShaderGraph on OSX_OpenGLCore_editmode_mono_Linear on version 2020.3
    agent:
        type: Unity::VM::osx
        image: graphics-foundation/mac-10.15:v0.0.1-672710
        flavor: m1.mac
    variables:
        CACHE_ARGS: '{{cache.flags}}'
        CUSTOM_REVISION: '{{2020_3.changeset.id}}'
        TEST_FILTER: .*
        UPM_REGISTRY: https://artifactory-slo.bf.unity3d.com/artifactory/api/npm/upm-candidates
        UTR_VERSION: "current"
    commands:
      -  command: curl -s https://artifactory.prd.it.unity3d.com/artifactory/unity-tools-local/utr-standalone/utr --output utr
         retries: 2
      -  command: chmod +x utr
      -  command: brew tap --force-auto-update unity/unity git@github.cds.internal.unity3d.com:unity/homebrew-unity.git
         retries: 2
      -  command: brew install unity-downloader-cli
         retries: 2
      -  command: unity-downloader-cli --source-file unity_revision.txt -c editor -c il2cpp  --wait --published-only
         retries: 2
      -  command: ./utr  --artifacts_path=TestProjects/ShaderGraph/test-results --editor-location=.Editor --extra-editor-arg="-colorspace=Linear" --platform=editmode --scripting-backend=Mono2x --suite=editor --testfilter=$TEST_FILTER --testproject=./TestProjects/ShaderGraph --zero-tests-are-ok=1
    artifacts:
        logs:
            paths:
              -  "**/test-results/**"
              -  "TestProjects/ShaderGraph/Logs/*.log"
    dependencies:
      -  path: .yamato/_editor_priming.yml#editor:priming:2020.3:OSX
         rerun: on_new_revision
ShaderGraph_OSX_OpenGLCore_playmode_mono_Linear_CUSTOM-REVISION:
    name: ShaderGraph on OSX_OpenGLCore_playmode_mono_Linear on version CUSTOM-REVISION
    agent:
        type: Unity::VM::osx
        image: graphics-foundation/mac-10.15:v0.0.1-672710
        flavor: m1.mac
    variables:
        CACHE_ARGS: '{{cache.flags}}'
        CUSTOM_REVISION: custom_revision_not_set
        TEST_FILTER: .*
        UPM_REGISTRY: https://artifactory-slo.bf.unity3d.com/artifactory/api/npm/upm-candidates
        UTR_VERSION: "current"
    commands:
      -  command: curl -s https://artifactory.prd.it.unity3d.com/artifactory/unity-tools-local/utr-standalone/utr --output utr
         retries: 2
      -  command: chmod +x utr
      -  command: brew tap --force-auto-update unity/unity git@github.cds.internal.unity3d.com:unity/homebrew-unity.git
         retries: 2
      -  command: brew install unity-downloader-cli
         retries: 2
      -  command: unity-downloader-cli --source-file unity_revision.txt -c editor -c il2cpp  --wait --published-only
         retries: 2
      -  command: ./utr  --artifacts_path=TestProjects/ShaderGraph/test-results --editor-location=.Editor --extra-editor-arg="-colorspace=Linear" --scripting-backend=Mono2x --suite=playmode --testfilter=$TEST_FILTER --testproject=./TestProjects/ShaderGraph --zero-tests-are-ok=1
    artifacts:
        logs:
            paths:
              -  "**/test-results/**"
              -  "TestProjects/ShaderGraph/Logs/*.log"
    dependencies:
      -  path: .yamato/_editor_priming.yml#editor:priming:CUSTOM-REVISION:OSX
         rerun: on_new_revision
ShaderGraph_OSX_OpenGLCore_editmode_mono_Linear_CUSTOM-REVISION:
    name: ShaderGraph on OSX_OpenGLCore_editmode_mono_Linear on version CUSTOM-REVISION
    agent:
        type: Unity::VM::osx
        image: graphics-foundation/mac-10.15:v0.0.1-672710
        flavor: m1.mac
    variables:
        CACHE_ARGS: '{{cache.flags}}'
        CUSTOM_REVISION: custom_revision_not_set
        TEST_FILTER: .*
        UPM_REGISTRY: https://artifactory-slo.bf.unity3d.com/artifactory/api/npm/upm-candidates
        UTR_VERSION: "current"
    commands:
      -  command: curl -s https://artifactory.prd.it.unity3d.com/artifactory/unity-tools-local/utr-standalone/utr --output utr
         retries: 2
      -  command: chmod +x utr
      -  command: brew tap --force-auto-update unity/unity git@github.cds.internal.unity3d.com:unity/homebrew-unity.git
         retries: 2
      -  command: brew install unity-downloader-cli
         retries: 2
      -  command: unity-downloader-cli --source-file unity_revision.txt -c editor -c il2cpp  --wait --published-only
         retries: 2
      -  command: ./utr  --artifacts_path=TestProjects/ShaderGraph/test-results --editor-location=.Editor --extra-editor-arg="-colorspace=Linear" --platform=editmode --scripting-backend=Mono2x --suite=editor --testfilter=$TEST_FILTER --testproject=./TestProjects/ShaderGraph --zero-tests-are-ok=1
    artifacts:
        logs:
            paths:
              -  "**/test-results/**"
              -  "TestProjects/ShaderGraph/Logs/*.log"
    dependencies:
      -  path: .yamato/_editor_priming.yml#editor:priming:CUSTOM-REVISION:OSX
         rerun: on_new_revision
