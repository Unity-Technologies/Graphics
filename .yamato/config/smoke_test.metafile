project:
  name: Smoke_Test
  folder: SRP_SmokeTest
test_platforms:
  - playmode
  - editmode
  - Standalone
platforms:
  - name: Win
    apis:
      - DX11
    overrides:
      agents_project:
        default:
          type: Unity::VM::GPU
          image: sdet/gamecode_win10:stable
          flavor: b1.large
        editmode:
          type: Unity::VM
          image: sdet/gamecode_win10:stable
          flavor: b1.large
        standalone:
          type: Unity::VM::GPU
          image: sdet/gamecode_win10:stable
          flavor: b1.xlarge
        standalone_build:
          type: Unity::VM::GPU
          image: sdet/gamecode_win10:stable
          flavor: b1.xlarge
all:
  dependencies:
    - platform: Win
      api: DX11
      test_platforms:
        - playmode
        - editmode
        - Standalone
        