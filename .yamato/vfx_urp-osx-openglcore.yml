
# This file is generated by .yamato/ruamel/build.py. Don't edit this file manually.
# Introduce any changes under .yamato/config/*.metafile files (for most cases) or under .yamato/ruamel/* within Python (more elaborate cases), and rerun build.py to regenerate all .yml files.
# Read more under .yamato/docs/readme.md

{% metadata_file .yamato/_cache_config.metafile -%}
{% metadata_file .yamato/_latest_editor_versions_2021.2.metafile -%}
{% metadata_file .yamato/_latest_editor_versions_trunk.metafile -%}

---

VFX_URP_OSX_OpenGLCore_Standalone_PerformanceTests_mono_Linear_trunk:
    name: VFX_URP on OSX_OpenGLCore_Standalone_PerformanceTests_mono_Linear on version trunk
    agent:
        type: Unity::VM::osx
        image: graphics-foundation/mac-10.15:v0.1.0-841394
        flavor: m1.mac
    variables:
        UPM_REGISTRY: https://artifactory-slo.bf.unity3d.com/artifactory/api/npm/upm-candidates
        CUSTOM_REVISION: '{{trunk.changeset.id}}'
        CACHE_ARGS: '{{cache.flags}}'
        UTR_VERSION: "current"
        TEST_FILTER: .*
    commands:
      -  command: curl -s https://artifactory.prd.it.unity3d.com/artifactory/unity-tools-local/utr-standalone/utr --output utr
         retries: 2
      -  chmod +x utr
      -  command: ./utr --artifacts_path=TestProjects/VisualEffectGraph_URP/test-results --performance-project-version=. --report-performance-data --performance-project-id=VFX --platform=StandaloneOSX --player-load-path=players --suite=playmode --timeout=1200 --zero-tests-are-ok=1
         retries: 2
    after:
      -  command: python3 .yamato/script/log_scripts/log_parser.py
    artifacts:
        logs:
            paths:
              -  "**/test-results/**"
              -  "TestProjects/VisualEffectGraph_URP/Logs/*.log"
    dependencies:
      -  path: .yamato/_editor_priming.yml#editor:priming:trunk:OSX
         rerun: on_new_revision
      -  path: .yamato/vfx_urp-osx-openglcore.yml#Build_VFX_URP_OSX_OpenGLCore_Standalone_PerformanceTests_mono_Linear_trunk
         rerun: on_new_revision
Build_VFX_URP_OSX_OpenGLCore_Standalone_PerformanceTests_mono_Linear_trunk:
    name: Build VFX_URP on OSX_OpenGLCore_mono_Linear_Standalone_PerformanceTests_build_Player on version trunk
    agent:
        type: Unity::VM::osx
        image: graphics-foundation/mac-10.15:v0.1.0-841394
        flavor: m1.mac
    variables:
        UPM_REGISTRY: https://artifactory-slo.bf.unity3d.com/artifactory/api/npm/upm-candidates
        CUSTOM_REVISION: '{{trunk.changeset.id}}'
        CACHE_ARGS: '{{cache.flags}}'
        UTR_VERSION: "current"
        TEST_FILTER: .*
    commands:
      -  command: curl -s https://artifactory.prd.it.unity3d.com/artifactory/unity-tools-local/utr-standalone/utr --output utr
         retries: 2
      -  chmod +x utr
      -  command: brew tap --force-auto-update unity/unity git@github.cds.internal.unity3d.com:unity/homebrew-unity.git
      -  command: brew install unity-downloader-cli
         retries: 2
      -  command: unity-downloader-cli --source-file unity_revision.txt -c editor -c il2cpp  --wait --published-only
         retries: 2
      -  command: ./utr $CACHE_ARGS --artifacts_path=TestProjects/VisualEffectGraph_URP/test-results --build-only --editor-location=.Editor --extra-editor-arg="-colorspace=Linear" --platform=StandaloneOSX --player-save-path=players --scripting-backend=Mono2x --suite=playmode --testfilter="VFX.PerformanceTest" --testproject=./TestProjects/VisualEffectGraph_URP --timeout=1200
         retries: 2
    after:
      -  command: python3 .yamato/script/log_scripts/log_parser.py
    artifacts:
        logs:
            paths:
              -  "**/test-results/**"
              -  "TestProjects/VisualEffectGraph_URP/Logs/*.log"
        players:
            paths:
              -  "players*/**"
    dependencies:
      -  path: .yamato/_editor_priming.yml#editor:priming:trunk:OSX
         rerun: on_new_revision
VFX_URP_OSX_OpenGLCore_playmode_mono_Linear_trunk:
    name: VFX_URP on OSX_OpenGLCore_playmode_mono_Linear on version trunk
    agent:
        type: Unity::VM::osx
        image: graphics-foundation/mac-10.15:v0.1.0-841394
        flavor: m1.mac
    variables:
        UPM_REGISTRY: https://artifactory-slo.bf.unity3d.com/artifactory/api/npm/upm-candidates
        CUSTOM_REVISION: '{{trunk.changeset.id}}'
        CACHE_ARGS: '{{cache.flags}}'
        UTR_VERSION: "current"
        TEST_FILTER: .*
    commands:
      -  command: curl -s https://artifactory.prd.it.unity3d.com/artifactory/unity-tools-local/utr-standalone/utr --output utr
         retries: 2
      -  chmod +x utr
      -  command: brew tap --force-auto-update unity/unity git@github.cds.internal.unity3d.com:unity/homebrew-unity.git
      -  command: brew install unity-downloader-cli
         retries: 2
      -  command: unity-downloader-cli --source-file unity_revision.txt -c editor -c il2cpp  --wait --published-only
         retries: 2
      -  command: ./utr $CACHE_ARGS --artifacts_path=TestProjects/VisualEffectGraph_URP/test-results --editor-location=.Editor --extra-editor-arg="-colorspace=Linear" --scripting-backend=Mono2x --suite=playmode --testfilter="VFX.Test" --testproject=./TestProjects/VisualEffectGraph_URP --zero-tests-are-ok=1
         retries: 2
    after:
      -  command: python3 .yamato/script/log_scripts/log_parser.py
    artifacts:
        logs:
            paths:
              -  "**/test-results/**"
              -  "TestProjects/VisualEffectGraph_URP/Logs/*.log"
    dependencies:
      -  path: .yamato/_editor_priming.yml#editor:priming:trunk:OSX
         rerun: on_new_revision
VFX_URP_OSX_OpenGLCore_playmode_PerformanceTests_mono_Linear_trunk:
    name: VFX_URP on OSX_OpenGLCore_playmode_PerformanceTests_mono_Linear on version trunk
    agent:
        type: Unity::VM::osx
        image: graphics-foundation/mac-10.15:v0.1.0-841394
        flavor: m1.mac
    variables:
        UPM_REGISTRY: https://artifactory-slo.bf.unity3d.com/artifactory/api/npm/upm-candidates
        CUSTOM_REVISION: '{{trunk.changeset.id}}'
        CACHE_ARGS: '{{cache.flags}}'
        UTR_VERSION: "current"
        TEST_FILTER: .*
    commands:
      -  command: curl -s https://artifactory.prd.it.unity3d.com/artifactory/unity-tools-local/utr-standalone/utr --output utr
         retries: 2
      -  chmod +x utr
      -  command: brew tap --force-auto-update unity/unity git@github.cds.internal.unity3d.com:unity/homebrew-unity.git
      -  command: brew install unity-downloader-cli
         retries: 2
      -  command: unity-downloader-cli --source-file unity_revision.txt -c editor -c il2cpp  --wait --published-only
         retries: 2
      -  command: ./utr $CACHE_ARGS --artifacts_path=TestProjects/VisualEffectGraph_URP/test-results --editor-location=.Editor --extra-editor-arg="-colorspace=Linear" --performance-project-version=. --report-performance-data --performance-project-id=VFX --scripting-backend=Mono2x --suite=playmode --testfilter="VFX.PerformanceTest" --testproject=./TestProjects/VisualEffectGraph_URP --zero-tests-are-ok=1
         retries: 2
    after:
      -  command: python3 .yamato/script/log_scripts/log_parser.py
    artifacts:
        logs:
            paths:
              -  "**/test-results/**"
              -  "TestProjects/VisualEffectGraph_URP/Logs/*.log"
    dependencies:
      -  path: .yamato/_editor_priming.yml#editor:priming:trunk:OSX
         rerun: on_new_revision
VFX_URP_OSX_OpenGLCore_editmode_mono_Linear_trunk:
    name: VFX_URP on OSX_OpenGLCore_editmode_mono_Linear on version trunk
    agent:
        type: Unity::VM::osx
        image: graphics-foundation/mac-10.15:v0.1.0-841394
        flavor: m1.mac
    variables:
        UPM_REGISTRY: https://artifactory-slo.bf.unity3d.com/artifactory/api/npm/upm-candidates
        CUSTOM_REVISION: '{{trunk.changeset.id}}'
        CACHE_ARGS: '{{cache.flags}}'
        UTR_VERSION: "current"
        TEST_FILTER: .*
    commands:
      -  command: curl -s https://artifactory.prd.it.unity3d.com/artifactory/unity-tools-local/utr-standalone/utr --output utr
         retries: 2
      -  chmod +x utr
      -  command: brew tap --force-auto-update unity/unity git@github.cds.internal.unity3d.com:unity/homebrew-unity.git
      -  command: brew install unity-downloader-cli
         retries: 2
      -  command: unity-downloader-cli --source-file unity_revision.txt -c editor -c il2cpp  --wait --published-only
         retries: 2
      -  command: ./utr --artifacts_path=TestProjects/VisualEffectGraph_URP/test-results --editor-location=.Editor --extra-editor-arg="-colorspace=Linear" --platform=editmode --scripting-backend=Mono2x --suite=editor --testfilter="VFX.Test" --testproject=./TestProjects/VisualEffectGraph_URP --zero-tests-are-ok=1
         retries: 2
    after:
      -  command: python3 .yamato/script/log_scripts/log_parser.py
    artifacts:
        logs:
            paths:
              -  "**/test-results/**"
              -  "TestProjects/VisualEffectGraph_URP/Logs/*.log"
    dependencies:
      -  path: .yamato/_editor_priming.yml#editor:priming:trunk:OSX
         rerun: on_new_revision
VFX_URP_OSX_OpenGLCore_editmode_PerformanceTests_mono_Linear_trunk:
    name: VFX_URP on OSX_OpenGLCore_editmode_PerformanceTests_mono_Linear on version trunk
    agent:
        type: Unity::VM::osx
        image: graphics-foundation/mac-10.15:v0.1.0-841394
        flavor: m1.mac
    variables:
        UPM_REGISTRY: https://artifactory-slo.bf.unity3d.com/artifactory/api/npm/upm-candidates
        CUSTOM_REVISION: '{{trunk.changeset.id}}'
        CACHE_ARGS: '{{cache.flags}}'
        UTR_VERSION: "current"
        TEST_FILTER: .*
    commands:
      -  command: curl -s https://artifactory.prd.it.unity3d.com/artifactory/unity-tools-local/utr-standalone/utr --output utr
         retries: 2
      -  chmod +x utr
      -  command: brew tap --force-auto-update unity/unity git@github.cds.internal.unity3d.com:unity/homebrew-unity.git
      -  command: brew install unity-downloader-cli
         retries: 2
      -  command: unity-downloader-cli --source-file unity_revision.txt -c editor -c il2cpp  --wait --published-only
         retries: 2
      -  command: ./utr --artifacts_path=TestProjects/VisualEffectGraph_URP/test-results --editor-location=.Editor --extra-editor-arg="-colorspace=Linear" --performance-project-version=. --report-performance-data --performance-project-id=VFX --platform=editmode --scripting-backend=Mono2x --suite=editor --testfilter="VFX.PerformanceTest" --testproject=./TestProjects/VisualEffectGraph_URP --zero-tests-are-ok=1
         retries: 2
    after:
      -  command: python3 .yamato/script/log_scripts/log_parser.py
    artifacts:
        logs:
            paths:
              -  "**/test-results/**"
              -  "TestProjects/VisualEffectGraph_URP/Logs/*.log"
    dependencies:
      -  path: .yamato/_editor_priming.yml#editor:priming:trunk:OSX
         rerun: on_new_revision
VFX_URP_OSX_OpenGLCore_Standalone_PerformanceTests_mono_Linear_2021.2:
    name: VFX_URP on OSX_OpenGLCore_Standalone_PerformanceTests_mono_Linear on version 2021.2
    agent:
        type: Unity::VM::osx
        image: graphics-foundation/mac-10.15:v0.1.0-841394
        flavor: m1.mac
    variables:
        UPM_REGISTRY: https://artifactory-slo.bf.unity3d.com/artifactory/api/npm/upm-candidates
        CUSTOM_REVISION: '{{2021_2.changeset.id}}'
        CACHE_ARGS: '{{cache.flags}}'
        UTR_VERSION: "current"
        TEST_FILTER: .*
    commands:
      -  command: curl -s https://artifactory.prd.it.unity3d.com/artifactory/unity-tools-local/utr-standalone/utr --output utr
         retries: 2
      -  chmod +x utr
      -  command: ./utr --artifacts_path=TestProjects/VisualEffectGraph_URP/test-results --performance-project-version=. --report-performance-data --performance-project-id=VFX --platform=StandaloneOSX --player-load-path=players --suite=playmode --timeout=1200 --zero-tests-are-ok=1
         retries: 2
    after:
      -  command: python3 .yamato/script/log_scripts/log_parser.py
    artifacts:
        logs:
            paths:
              -  "**/test-results/**"
              -  "TestProjects/VisualEffectGraph_URP/Logs/*.log"
    dependencies:
      -  path: .yamato/_editor_priming.yml#editor:priming:2021.2:OSX
         rerun: on_new_revision
      -  path: .yamato/vfx_urp-osx-openglcore.yml#Build_VFX_URP_OSX_OpenGLCore_Standalone_PerformanceTests_mono_Linear_2021.2
         rerun: on_new_revision
Build_VFX_URP_OSX_OpenGLCore_Standalone_PerformanceTests_mono_Linear_2021.2:
    name: Build VFX_URP on OSX_OpenGLCore_mono_Linear_Standalone_PerformanceTests_build_Player on version 2021.2
    agent:
        type: Unity::VM::osx
        image: graphics-foundation/mac-10.15:v0.1.0-841394
        flavor: m1.mac
    variables:
        UPM_REGISTRY: https://artifactory-slo.bf.unity3d.com/artifactory/api/npm/upm-candidates
        CUSTOM_REVISION: '{{2021_2.changeset.id}}'
        CACHE_ARGS: '{{cache.flags}}'
        UTR_VERSION: "current"
        TEST_FILTER: .*
    commands:
      -  command: curl -s https://artifactory.prd.it.unity3d.com/artifactory/unity-tools-local/utr-standalone/utr --output utr
         retries: 2
      -  chmod +x utr
      -  command: brew tap --force-auto-update unity/unity git@github.cds.internal.unity3d.com:unity/homebrew-unity.git
      -  command: brew install unity-downloader-cli
         retries: 2
      -  command: unity-downloader-cli --source-file unity_revision.txt -c editor -c il2cpp  --wait --published-only
         retries: 2
      -  command: ./utr $CACHE_ARGS --artifacts_path=TestProjects/VisualEffectGraph_URP/test-results --build-only --editor-location=.Editor --extra-editor-arg="-colorspace=Linear" --platform=StandaloneOSX --player-save-path=players --scripting-backend=Mono2x --suite=playmode --testfilter="VFX.PerformanceTest" --testproject=./TestProjects/VisualEffectGraph_URP --timeout=1200
         retries: 2
    after:
      -  command: python3 .yamato/script/log_scripts/log_parser.py
    artifacts:
        logs:
            paths:
              -  "**/test-results/**"
              -  "TestProjects/VisualEffectGraph_URP/Logs/*.log"
        players:
            paths:
              -  "players*/**"
    dependencies:
      -  path: .yamato/_editor_priming.yml#editor:priming:2021.2:OSX
         rerun: on_new_revision
VFX_URP_OSX_OpenGLCore_playmode_mono_Linear_2021.2:
    name: VFX_URP on OSX_OpenGLCore_playmode_mono_Linear on version 2021.2
    agent:
        type: Unity::VM::osx
        image: graphics-foundation/mac-10.15:v0.1.0-841394
        flavor: m1.mac
    variables:
        UPM_REGISTRY: https://artifactory-slo.bf.unity3d.com/artifactory/api/npm/upm-candidates
        CUSTOM_REVISION: '{{2021_2.changeset.id}}'
        CACHE_ARGS: '{{cache.flags}}'
        UTR_VERSION: "current"
        TEST_FILTER: .*
    commands:
      -  command: curl -s https://artifactory.prd.it.unity3d.com/artifactory/unity-tools-local/utr-standalone/utr --output utr
         retries: 2
      -  chmod +x utr
      -  command: brew tap --force-auto-update unity/unity git@github.cds.internal.unity3d.com:unity/homebrew-unity.git
      -  command: brew install unity-downloader-cli
         retries: 2
      -  command: unity-downloader-cli --source-file unity_revision.txt -c editor -c il2cpp  --wait --published-only
         retries: 2
      -  command: ./utr $CACHE_ARGS --artifacts_path=TestProjects/VisualEffectGraph_URP/test-results --editor-location=.Editor --extra-editor-arg="-colorspace=Linear" --scripting-backend=Mono2x --suite=playmode --testfilter="VFX.Test" --testproject=./TestProjects/VisualEffectGraph_URP --zero-tests-are-ok=1
         retries: 2
    after:
      -  command: python3 .yamato/script/log_scripts/log_parser.py
    artifacts:
        logs:
            paths:
              -  "**/test-results/**"
              -  "TestProjects/VisualEffectGraph_URP/Logs/*.log"
    dependencies:
      -  path: .yamato/_editor_priming.yml#editor:priming:2021.2:OSX
         rerun: on_new_revision
VFX_URP_OSX_OpenGLCore_playmode_PerformanceTests_mono_Linear_2021.2:
    name: VFX_URP on OSX_OpenGLCore_playmode_PerformanceTests_mono_Linear on version 2021.2
    agent:
        type: Unity::VM::osx
        image: graphics-foundation/mac-10.15:v0.1.0-841394
        flavor: m1.mac
    variables:
        UPM_REGISTRY: https://artifactory-slo.bf.unity3d.com/artifactory/api/npm/upm-candidates
        CUSTOM_REVISION: '{{2021_2.changeset.id}}'
        CACHE_ARGS: '{{cache.flags}}'
        UTR_VERSION: "current"
        TEST_FILTER: .*
    commands:
      -  command: curl -s https://artifactory.prd.it.unity3d.com/artifactory/unity-tools-local/utr-standalone/utr --output utr
         retries: 2
      -  chmod +x utr
      -  command: brew tap --force-auto-update unity/unity git@github.cds.internal.unity3d.com:unity/homebrew-unity.git
      -  command: brew install unity-downloader-cli
         retries: 2
      -  command: unity-downloader-cli --source-file unity_revision.txt -c editor -c il2cpp  --wait --published-only
         retries: 2
      -  command: ./utr $CACHE_ARGS --artifacts_path=TestProjects/VisualEffectGraph_URP/test-results --editor-location=.Editor --extra-editor-arg="-colorspace=Linear" --performance-project-version=. --report-performance-data --performance-project-id=VFX --scripting-backend=Mono2x --suite=playmode --testfilter="VFX.PerformanceTest" --testproject=./TestProjects/VisualEffectGraph_URP --zero-tests-are-ok=1
         retries: 2
    after:
      -  command: python3 .yamato/script/log_scripts/log_parser.py
    artifacts:
        logs:
            paths:
              -  "**/test-results/**"
              -  "TestProjects/VisualEffectGraph_URP/Logs/*.log"
    dependencies:
      -  path: .yamato/_editor_priming.yml#editor:priming:2021.2:OSX
         rerun: on_new_revision
VFX_URP_OSX_OpenGLCore_editmode_mono_Linear_2021.2:
    name: VFX_URP on OSX_OpenGLCore_editmode_mono_Linear on version 2021.2
    agent:
        type: Unity::VM::osx
        image: graphics-foundation/mac-10.15:v0.1.0-841394
        flavor: m1.mac
    variables:
        UPM_REGISTRY: https://artifactory-slo.bf.unity3d.com/artifactory/api/npm/upm-candidates
        CUSTOM_REVISION: '{{2021_2.changeset.id}}'
        CACHE_ARGS: '{{cache.flags}}'
        UTR_VERSION: "current"
        TEST_FILTER: .*
    commands:
      -  command: curl -s https://artifactory.prd.it.unity3d.com/artifactory/unity-tools-local/utr-standalone/utr --output utr
         retries: 2
      -  chmod +x utr
      -  command: brew tap --force-auto-update unity/unity git@github.cds.internal.unity3d.com:unity/homebrew-unity.git
      -  command: brew install unity-downloader-cli
         retries: 2
      -  command: unity-downloader-cli --source-file unity_revision.txt -c editor -c il2cpp  --wait --published-only
         retries: 2
      -  command: ./utr --artifacts_path=TestProjects/VisualEffectGraph_URP/test-results --editor-location=.Editor --extra-editor-arg="-colorspace=Linear" --platform=editmode --scripting-backend=Mono2x --suite=editor --testfilter="VFX.Test" --testproject=./TestProjects/VisualEffectGraph_URP --zero-tests-are-ok=1
         retries: 2
    after:
      -  command: python3 .yamato/script/log_scripts/log_parser.py
    artifacts:
        logs:
            paths:
              -  "**/test-results/**"
              -  "TestProjects/VisualEffectGraph_URP/Logs/*.log"
    dependencies:
      -  path: .yamato/_editor_priming.yml#editor:priming:2021.2:OSX
         rerun: on_new_revision
VFX_URP_OSX_OpenGLCore_editmode_PerformanceTests_mono_Linear_2021.2:
    name: VFX_URP on OSX_OpenGLCore_editmode_PerformanceTests_mono_Linear on version 2021.2
    agent:
        type: Unity::VM::osx
        image: graphics-foundation/mac-10.15:v0.1.0-841394
        flavor: m1.mac
    variables:
        UPM_REGISTRY: https://artifactory-slo.bf.unity3d.com/artifactory/api/npm/upm-candidates
        CUSTOM_REVISION: '{{2021_2.changeset.id}}'
        CACHE_ARGS: '{{cache.flags}}'
        UTR_VERSION: "current"
        TEST_FILTER: .*
    commands:
      -  command: curl -s https://artifactory.prd.it.unity3d.com/artifactory/unity-tools-local/utr-standalone/utr --output utr
         retries: 2
      -  chmod +x utr
      -  command: brew tap --force-auto-update unity/unity git@github.cds.internal.unity3d.com:unity/homebrew-unity.git
      -  command: brew install unity-downloader-cli
         retries: 2
      -  command: unity-downloader-cli --source-file unity_revision.txt -c editor -c il2cpp  --wait --published-only
         retries: 2
      -  command: ./utr --artifacts_path=TestProjects/VisualEffectGraph_URP/test-results --editor-location=.Editor --extra-editor-arg="-colorspace=Linear" --performance-project-version=. --report-performance-data --performance-project-id=VFX --platform=editmode --scripting-backend=Mono2x --suite=editor --testfilter="VFX.PerformanceTest" --testproject=./TestProjects/VisualEffectGraph_URP --zero-tests-are-ok=1
         retries: 2
    after:
      -  command: python3 .yamato/script/log_scripts/log_parser.py
    artifacts:
        logs:
            paths:
              -  "**/test-results/**"
              -  "TestProjects/VisualEffectGraph_URP/Logs/*.log"
    dependencies:
      -  path: .yamato/_editor_priming.yml#editor:priming:2021.2:OSX
         rerun: on_new_revision
VFX_URP_OSX_OpenGLCore_Standalone_PerformanceTests_mono_Linear_CUSTOM-REVISION:
    name: VFX_URP on OSX_OpenGLCore_Standalone_PerformanceTests_mono_Linear on version CUSTOM-REVISION
    agent:
        type: Unity::VM::osx
        image: graphics-foundation/mac-10.15:v0.1.0-841394
        flavor: m1.mac
    variables:
        UPM_REGISTRY: https://artifactory-slo.bf.unity3d.com/artifactory/api/npm/upm-candidates
        CUSTOM_REVISION: custom_revision_not_set
        CACHE_ARGS: '{{cache.flags}}'
        UTR_VERSION: "current"
        TEST_FILTER: .*
    commands:
      -  command: curl -s https://artifactory.prd.it.unity3d.com/artifactory/unity-tools-local/utr-standalone/utr --output utr
         retries: 2
      -  chmod +x utr
      -  command: ./utr --artifacts_path=TestProjects/VisualEffectGraph_URP/test-results --performance-project-version=. --report-performance-data --performance-project-id=VFX --platform=StandaloneOSX --player-load-path=players --suite=playmode --timeout=1200 --zero-tests-are-ok=1
         retries: 2
    after:
      -  command: python3 .yamato/script/log_scripts/log_parser.py
    artifacts:
        logs:
            paths:
              -  "**/test-results/**"
              -  "TestProjects/VisualEffectGraph_URP/Logs/*.log"
    dependencies:
      -  path: .yamato/_editor_priming.yml#editor:priming:CUSTOM-REVISION:OSX
         rerun: on_new_revision
      -  path: .yamato/vfx_urp-osx-openglcore.yml#Build_VFX_URP_OSX_OpenGLCore_Standalone_PerformanceTests_mono_Linear_CUSTOM-REVISION
         rerun: on_new_revision
Build_VFX_URP_OSX_OpenGLCore_Standalone_PerformanceTests_mono_Linear_CUSTOM-REVISION:
    name: Build VFX_URP on OSX_OpenGLCore_mono_Linear_Standalone_PerformanceTests_build_Player on version CUSTOM-REVISION
    agent:
        type: Unity::VM::osx
        image: graphics-foundation/mac-10.15:v0.1.0-841394
        flavor: m1.mac
    variables:
        UPM_REGISTRY: https://artifactory-slo.bf.unity3d.com/artifactory/api/npm/upm-candidates
        CUSTOM_REVISION: custom_revision_not_set
        CACHE_ARGS: '{{cache.flags}}'
        UTR_VERSION: "current"
        TEST_FILTER: .*
    commands:
      -  command: curl -s https://artifactory.prd.it.unity3d.com/artifactory/unity-tools-local/utr-standalone/utr --output utr
         retries: 2
      -  chmod +x utr
      -  command: brew tap --force-auto-update unity/unity git@github.cds.internal.unity3d.com:unity/homebrew-unity.git
      -  command: brew install unity-downloader-cli
         retries: 2
      -  command: unity-downloader-cli --source-file unity_revision.txt -c editor -c il2cpp  --wait --published-only
         retries: 2
      -  command: ./utr $CACHE_ARGS --artifacts_path=TestProjects/VisualEffectGraph_URP/test-results --build-only --editor-location=.Editor --extra-editor-arg="-colorspace=Linear" --platform=StandaloneOSX --player-save-path=players --scripting-backend=Mono2x --suite=playmode --testfilter="VFX.PerformanceTest" --testproject=./TestProjects/VisualEffectGraph_URP --timeout=1200
         retries: 2
    after:
      -  command: python3 .yamato/script/log_scripts/log_parser.py
    artifacts:
        logs:
            paths:
              -  "**/test-results/**"
              -  "TestProjects/VisualEffectGraph_URP/Logs/*.log"
        players:
            paths:
              -  "players*/**"
    dependencies:
      -  path: .yamato/_editor_priming.yml#editor:priming:CUSTOM-REVISION:OSX
         rerun: on_new_revision
VFX_URP_OSX_OpenGLCore_playmode_mono_Linear_CUSTOM-REVISION:
    name: VFX_URP on OSX_OpenGLCore_playmode_mono_Linear on version CUSTOM-REVISION
    agent:
        type: Unity::VM::osx
        image: graphics-foundation/mac-10.15:v0.1.0-841394
        flavor: m1.mac
    variables:
        UPM_REGISTRY: https://artifactory-slo.bf.unity3d.com/artifactory/api/npm/upm-candidates
        CUSTOM_REVISION: custom_revision_not_set
        CACHE_ARGS: '{{cache.flags}}'
        UTR_VERSION: "current"
        TEST_FILTER: .*
    commands:
      -  command: curl -s https://artifactory.prd.it.unity3d.com/artifactory/unity-tools-local/utr-standalone/utr --output utr
         retries: 2
      -  chmod +x utr
      -  command: brew tap --force-auto-update unity/unity git@github.cds.internal.unity3d.com:unity/homebrew-unity.git
      -  command: brew install unity-downloader-cli
         retries: 2
      -  command: unity-downloader-cli --source-file unity_revision.txt -c editor -c il2cpp  --wait --published-only
         retries: 2
      -  command: ./utr $CACHE_ARGS --artifacts_path=TestProjects/VisualEffectGraph_URP/test-results --editor-location=.Editor --extra-editor-arg="-colorspace=Linear" --scripting-backend=Mono2x --suite=playmode --testfilter="VFX.Test" --testproject=./TestProjects/VisualEffectGraph_URP --zero-tests-are-ok=1
         retries: 2
    after:
      -  command: python3 .yamato/script/log_scripts/log_parser.py
    artifacts:
        logs:
            paths:
              -  "**/test-results/**"
              -  "TestProjects/VisualEffectGraph_URP/Logs/*.log"
    dependencies:
      -  path: .yamato/_editor_priming.yml#editor:priming:CUSTOM-REVISION:OSX
         rerun: on_new_revision
VFX_URP_OSX_OpenGLCore_playmode_PerformanceTests_mono_Linear_CUSTOM-REVISION:
    name: VFX_URP on OSX_OpenGLCore_playmode_PerformanceTests_mono_Linear on version CUSTOM-REVISION
    agent:
        type: Unity::VM::osx
        image: graphics-foundation/mac-10.15:v0.1.0-841394
        flavor: m1.mac
    variables:
        UPM_REGISTRY: https://artifactory-slo.bf.unity3d.com/artifactory/api/npm/upm-candidates
        CUSTOM_REVISION: custom_revision_not_set
        CACHE_ARGS: '{{cache.flags}}'
        UTR_VERSION: "current"
        TEST_FILTER: .*
    commands:
      -  command: curl -s https://artifactory.prd.it.unity3d.com/artifactory/unity-tools-local/utr-standalone/utr --output utr
         retries: 2
      -  chmod +x utr
      -  command: brew tap --force-auto-update unity/unity git@github.cds.internal.unity3d.com:unity/homebrew-unity.git
      -  command: brew install unity-downloader-cli
         retries: 2
      -  command: unity-downloader-cli --source-file unity_revision.txt -c editor -c il2cpp  --wait --published-only
         retries: 2
      -  command: ./utr $CACHE_ARGS --artifacts_path=TestProjects/VisualEffectGraph_URP/test-results --editor-location=.Editor --extra-editor-arg="-colorspace=Linear" --performance-project-version=. --report-performance-data --performance-project-id=VFX --scripting-backend=Mono2x --suite=playmode --testfilter="VFX.PerformanceTest" --testproject=./TestProjects/VisualEffectGraph_URP --zero-tests-are-ok=1
         retries: 2
    after:
      -  command: python3 .yamato/script/log_scripts/log_parser.py
    artifacts:
        logs:
            paths:
              -  "**/test-results/**"
              -  "TestProjects/VisualEffectGraph_URP/Logs/*.log"
    dependencies:
      -  path: .yamato/_editor_priming.yml#editor:priming:CUSTOM-REVISION:OSX
         rerun: on_new_revision
VFX_URP_OSX_OpenGLCore_editmode_mono_Linear_CUSTOM-REVISION:
    name: VFX_URP on OSX_OpenGLCore_editmode_mono_Linear on version CUSTOM-REVISION
    agent:
        type: Unity::VM::osx
        image: graphics-foundation/mac-10.15:v0.1.0-841394
        flavor: m1.mac
    variables:
        UPM_REGISTRY: https://artifactory-slo.bf.unity3d.com/artifactory/api/npm/upm-candidates
        CUSTOM_REVISION: custom_revision_not_set
        CACHE_ARGS: '{{cache.flags}}'
        UTR_VERSION: "current"
        TEST_FILTER: .*
    commands:
      -  command: curl -s https://artifactory.prd.it.unity3d.com/artifactory/unity-tools-local/utr-standalone/utr --output utr
         retries: 2
      -  chmod +x utr
      -  command: brew tap --force-auto-update unity/unity git@github.cds.internal.unity3d.com:unity/homebrew-unity.git
      -  command: brew install unity-downloader-cli
         retries: 2
      -  command: unity-downloader-cli --source-file unity_revision.txt -c editor -c il2cpp  --wait --published-only
         retries: 2
      -  command: ./utr --artifacts_path=TestProjects/VisualEffectGraph_URP/test-results --editor-location=.Editor --extra-editor-arg="-colorspace=Linear" --platform=editmode --scripting-backend=Mono2x --suite=editor --testfilter="VFX.Test" --testproject=./TestProjects/VisualEffectGraph_URP --zero-tests-are-ok=1
         retries: 2
    after:
      -  command: python3 .yamato/script/log_scripts/log_parser.py
    artifacts:
        logs:
            paths:
              -  "**/test-results/**"
              -  "TestProjects/VisualEffectGraph_URP/Logs/*.log"
    dependencies:
      -  path: .yamato/_editor_priming.yml#editor:priming:CUSTOM-REVISION:OSX
         rerun: on_new_revision
VFX_URP_OSX_OpenGLCore_editmode_PerformanceTests_mono_Linear_CUSTOM-REVISION:
    name: VFX_URP on OSX_OpenGLCore_editmode_PerformanceTests_mono_Linear on version CUSTOM-REVISION
    agent:
        type: Unity::VM::osx
        image: graphics-foundation/mac-10.15:v0.1.0-841394
        flavor: m1.mac
    variables:
        UPM_REGISTRY: https://artifactory-slo.bf.unity3d.com/artifactory/api/npm/upm-candidates
        CUSTOM_REVISION: custom_revision_not_set
        CACHE_ARGS: '{{cache.flags}}'
        UTR_VERSION: "current"
        TEST_FILTER: .*
    commands:
      -  command: curl -s https://artifactory.prd.it.unity3d.com/artifactory/unity-tools-local/utr-standalone/utr --output utr
         retries: 2
      -  chmod +x utr
      -  command: brew tap --force-auto-update unity/unity git@github.cds.internal.unity3d.com:unity/homebrew-unity.git
      -  command: brew install unity-downloader-cli
         retries: 2
      -  command: unity-downloader-cli --source-file unity_revision.txt -c editor -c il2cpp  --wait --published-only
         retries: 2
      -  command: ./utr --artifacts_path=TestProjects/VisualEffectGraph_URP/test-results --editor-location=.Editor --extra-editor-arg="-colorspace=Linear" --performance-project-version=. --report-performance-data --performance-project-id=VFX --platform=editmode --scripting-backend=Mono2x --suite=editor --testfilter="VFX.PerformanceTest" --testproject=./TestProjects/VisualEffectGraph_URP --zero-tests-are-ok=1
         retries: 2
    after:
      -  command: python3 .yamato/script/log_scripts/log_parser.py
    artifacts:
        logs:
            paths:
              -  "**/test-results/**"
              -  "TestProjects/VisualEffectGraph_URP/Logs/*.log"
    dependencies:
      -  path: .yamato/_editor_priming.yml#editor:priming:CUSTOM-REVISION:OSX
         rerun: on_new_revision
