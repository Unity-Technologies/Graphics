 
# This file is generated by .yamato/ruamel/build.py. Don't edit this file manually. 
# Introduce any changes under .yamato/config/*.metafile files (for most cases) or under .yamato/ruamel/* within Python (more elaborate cases), and rerun build.py to regenerate all .yml files. 
# Read more under .yamato/docs/readme.md 

editor-pinning-target-to-ci:
    name: Merge master to ci/editor-pinning
    agent:
        type: Unity::VM
        image: package-ci/ubuntu:stable
        flavor: b1.small
    variables:
        CI: true
    commands:
      -  echo $GIT_BRANCH
      -  |5-

                     if [[ "$GIT_BRANCH" != "master" ]]; then
                         echo "Should run on 'master' but is running on '$GIT_BRANCH'"
                         exit 1
                     fi
      -  git fetch
      -  git checkout master
      -  git checkout ci/editor-pinning
      -  git config --global user.name "noreply@unity3d.com"
      -  git config --global user.email "noreply@unity3d.com"
      -  git merge master --no-ff -X theirs
      -  git push
editor-pinning-update:
    name: Update pinned editor versions
    agent:
        type: Unity::VM
        image: package-ci/ubuntu:stable
        flavor: b1.small
    variables:
        CI: true
    commands:
      -  sudo pip3 install pipenv --index-url https://artifactory.prd.it.unity3d.com/artifactory/api/pypi/pypi/simple
      -  python3 -m pipenv install --dev
      -  curl -L https://artifactory.prd.it.unity3d.com/artifactory/api/gpg/key/public | sudo apt-key add -
      -  sudo sh -c "echo 'deb https://artifactory.prd.it.unity3d.com/artifactory/unity-apt-local bionic main' > /etc/apt/sources.list.d/unity.list"
      -  sudo apt-get update
      -  sudo apt-get install yamato-parser -y
      -  |5-

                     if [[ "$GIT_BRANCH" != "master" ]]; then
                         echo "Should run on 'master' but is running on '$GIT_BRANCH'"
                         exit 1
                     fi
      -  git config --global user.name "noreply@unity3d.com"
      -  git config --global user.email "noreply@unity3d.com"
      -  pipenv run python3 .yamato/ruamel/editor_pinning/update_revisions.py --target-branch ci/editor-pinning --force-push
    dependencies:
      -  .yamato/_editor_pinning.yml#editor-pinning-target-to-ci
    triggers:
        recurring:
          -  branch: master
             frequency: 0 * * ?
editor-pinning-merge-revisions-trunk:
    name: Merge [trunk] revisions to master [no ABV]
    agent:
        type: Unity::VM
        image: package-ci/ubuntu:stable
        flavor: b1.small
    variables:
        CI: true
    commands:
      -  sudo pip3 install pipenv --index-url https://artifactory.prd.it.unity3d.com/artifactory/api/pypi/pypi/simple
      -  python3 -m pipenv install --dev
      -  curl -L https://artifactory.prd.it.unity3d.com/artifactory/api/gpg/key/public | sudo apt-key add -
      -  sudo sh -c "echo 'deb https://artifactory.prd.it.unity3d.com/artifactory/unity-apt-local bionic main' > /etc/apt/sources.list.d/unity.list"
      -  sudo apt-get update
      -  sudo apt-get install yamato-parser -y
      -  |5-

                     if [[ "$GIT_BRANCH" != "ci/editor-pinning" ]]; then
                         echo "Should run on 'ci/editor-pinning' but is running on '$GIT_BRANCH'"
                         exit 1
                     fi
      -  git config --global user.name "noreply@unity3d.com"
      -  git config --global user.email "noreply@unity3d.com"
      -  pipenv run python3 .yamato/ruamel/editor_pinning/merge_revisions.py --revision $GIT_REVISION --target-branch master --track trunk
editor-pinning-merge-revisions-trunk-abv:
    name: Merge [trunk] revisions to master [ABV]
    allow_failure: true
    dependencies:
      -  .yamato/_abv.yml#all_project_ci_trunk
    agent:
        type: Unity::VM
        image: package-ci/ubuntu:stable
        flavor: b1.small
    variables:
        CI: true
    commands:
      -  sudo pip3 install pipenv --index-url https://artifactory.prd.it.unity3d.com/artifactory/api/pypi/pypi/simple
      -  python3 -m pipenv install --dev
      -  curl -L https://artifactory.prd.it.unity3d.com/artifactory/api/gpg/key/public | sudo apt-key add -
      -  sudo sh -c "echo 'deb https://artifactory.prd.it.unity3d.com/artifactory/unity-apt-local bionic main' > /etc/apt/sources.list.d/unity.list"
      -  sudo apt-get update
      -  sudo apt-get install yamato-parser -y
      -  |5-

                     if [[ "$GIT_BRANCH" != "ci/editor-pinning" ]]; then
                         echo "Should run on 'ci/editor-pinning' but is running on '$GIT_BRANCH'"
                         exit 1
                     fi
      -  git config --global user.name "noreply@unity3d.com"
      -  git config --global user.email "noreply@unity3d.com"
      -  pipenv run python3 .yamato/ruamel/editor_pinning/merge_revisions.py --revision $GIT_REVISION --target-branch master --track trunk
editor-pinning-merge-all:
    name: Merge all [no ABV] [no CI]
    agent:
        type: Unity::VM
        image: package-ci/ubuntu:stable
        flavor: b1.small
    variables:
        CI: true
    dependencies:
      -  .yamato/_editor_pinning.yml#editor-pinning-merge-revisions-trunk
    commands:
      -  sudo pip3 install pipenv --index-url https://artifactory.prd.it.unity3d.com/artifactory/api/pypi/pypi/simple
      -  python3 -m pipenv install --dev
      -  curl -L https://artifactory.prd.it.unity3d.com/artifactory/api/gpg/key/public | sudo apt-key add -
      -  sudo sh -c "echo 'deb https://artifactory.prd.it.unity3d.com/artifactory/unity-apt-local bionic main' > /etc/apt/sources.list.d/unity.list"
      -  sudo apt-get update
      -  |5-

                     if [[ "$GIT_BRANCH" != "ci/editor-pinning" ]]; then
                         echo "Should run on 'ci/editor-pinning' but is running on '$GIT_BRANCH'"
                         exit 1
                     fi
      -  git config --global user.name "noreply@unity3d.com"
      -  git config --global user.email "noreply@unity3d.com"
      -  git checkout master
      -  git pull
      -  pipenv run python3 .yamato/ruamel/build.py
      -  git add .yamato/*.yml
      -  git commit -m "[CI] Updated .ymls to new revision" --allow-empty
      -  git push
editor-pinning-merge-all-abv:
    name: Merge all [ABV] [CI]
    triggers:
        expression: push.branch eq "ci/editor-pinning" AND push.changes.any match "**/_latest_editor_versions*.metafile"
    agent:
        type: Unity::VM
        image: package-ci/ubuntu:stable
        flavor: b1.small
    variables:
        CI: true
    dependencies:
      -  .yamato/_editor_pinning.yml#editor-pinning-merge-revisions-trunk-abv
    commands:
      -  sudo pip3 install pipenv --index-url https://artifactory.prd.it.unity3d.com/artifactory/api/pypi/pypi/simple
      -  python3 -m pipenv install --dev
      -  curl -L https://artifactory.prd.it.unity3d.com/artifactory/api/gpg/key/public | sudo apt-key add -
      -  sudo sh -c "echo 'deb https://artifactory.prd.it.unity3d.com/artifactory/unity-apt-local bionic main' > /etc/apt/sources.list.d/unity.list"
      -  sudo apt-get update
      -  |5-

                     if [[ "$GIT_BRANCH" != "ci/editor-pinning" ]]; then
                         echo "Should run on 'ci/editor-pinning' but is running on '$GIT_BRANCH'"
                         exit 1
                     fi
      -  git config --global user.name "noreply@unity3d.com"
      -  git config --global user.email "noreply@unity3d.com"
      -  git checkout master
      -  git pull
      -  pipenv run python3 .yamato/ruamel/build.py
      -  git add .yamato/*.yml
      -  git commit -m "[CI] Updated .ymls to new revision" --allow-empty
      -  git push
