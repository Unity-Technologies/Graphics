 
# This file is generated by .yamato/ruamel/build.py. Don't edit this file manually. 
# Introduce any changes under .yamato/config/*.metafile files (for most cases) or under .yamato/ruamel/* within Python (more elaborate cases), and rerun build.py to regenerate all .yml files. 
# Read more under .yamato/docs/readme.md 

{% metadata_file .yamato/_latest_editor_versions_trunk.metafile -%}

---

editor-pinning-target-to-ci:
    name: Merge yamato/old-package-ci-image to yamato/old-package-ci-image-ci
    agent:
        type: Unity::VM
        image: package-ci/ubuntu:v1.2.4-701242
        flavor: b1.small
    variables:
        CI: true
    commands:
      -  echo $GIT_BRANCH
      -  |5-

                     if [[ "$GIT_BRANCH" != "yamato/old-package-ci-image" ]]; then
                         echo "Should run on 'yamato/old-package-ci-image' but is running on '$GIT_BRANCH'"
                         exit 1
                     fi
      -  git fetch
      -  git checkout yamato/old-package-ci-image
      -  git checkout yamato/old-package-ci-image-ci
      -  git config --global user.name "noreply@unity3d.com"
      -  git config --global user.email "noreply@unity3d.com"
      -  git merge yamato/old-package-ci-image --no-ff -X theirs
      -  git push
editor-pinning-update:
    name: Update pinned editor versions
    agent:
        type: Unity::VM
        image: package-ci/ubuntu:v1.2.4-701242
        flavor: b1.small
    variables:
        CI: true
    commands:
      -  sudo pip3 install pipenv --index-url https://artifactory.prd.it.unity3d.com/artifactory/api/pypi/pypi/simple
      -  python3 -m pipenv install --dev
      -  curl -L https://artifactory.prd.it.unity3d.com/artifactory/api/gpg/key/public | sudo apt-key add -
      -  sudo sh -c "echo 'deb https://artifactory.prd.it.unity3d.com/artifactory/unity-apt-local bionic main' > /etc/apt/sources.list.d/unity.list"
      -  sudo apt-get update
      -  sudo apt-get install yamato-parser -y
      -  |5-

                     if [[ "$GIT_BRANCH" != "yamato/old-package-ci-image" ]]; then
                         echo "Should run on 'yamato/old-package-ci-image' but is running on '$GIT_BRANCH'"
                         exit 1
                     fi
      -  git config --global user.name "noreply@unity3d.com"
      -  git config --global user.email "noreply@unity3d.com"
      -  pipenv run python3 .yamato/script/editor_scripts/update_revisions.py --target-branch yamato/old-package-ci-image-ci
    dependencies:
      -  .yamato/_editor_pinning.yml#editor-pinning-target-to-ci
    triggers:
        recurring:
          -  branch: yamato/old-package-ci-image
             frequency: 23 * * ?
editor-pinning-merge-revisions-trunk:
    name: Merge [trunk] revisions to yamato/old-package-ci-image [no ABV]
    agent:
        type: Unity::VM
        image: package-ci/ubuntu:v1.2.4-701242
        flavor: b1.small
    variables:
        CI: true
    commands:
      -  sudo pip3 install pipenv --index-url https://artifactory.prd.it.unity3d.com/artifactory/api/pypi/pypi/simple
      -  python3 -m pipenv install --dev
      -  curl -L https://artifactory.prd.it.unity3d.com/artifactory/api/gpg/key/public | sudo apt-key add -
      -  sudo sh -c "echo 'deb https://artifactory.prd.it.unity3d.com/artifactory/unity-apt-local bionic main' > /etc/apt/sources.list.d/unity.list"
      -  sudo apt-get update
      -  sudo apt-get install yamato-parser -y
      -  |5-

                     if [[ "$GIT_BRANCH" != "yamato/old-package-ci-image-ci" ]]; then
                         echo "Should run on 'yamato/old-package-ci-image-ci' but is running on '$GIT_BRANCH'"
                         exit 1
                     fi
      -  git config --global user.name "noreply@unity3d.com"
      -  git config --global user.email "noreply@unity3d.com"
      -  pipenv run python3 .yamato/script/editor_scripts/merge_revisions.py --revision $GIT_REVISION --target-branch yamato/old-package-ci-image --track trunk
editor-pinning-merge-revisions-trunk-abv:
    name: Merge [trunk] revisions to yamato/old-package-ci-image [ABV]
    allow_failure: true
    dependencies:
      -  .yamato/_abv.yml#all_project_ci_trunk
    agent:
        type: Unity::VM
        image: package-ci/ubuntu:v1.2.4-701242
        flavor: b1.small
    variables:
        CI: true
    commands:
      -  sudo pip3 install pipenv --index-url https://artifactory.prd.it.unity3d.com/artifactory/api/pypi/pypi/simple
      -  python3 -m pipenv install --dev
      -  curl -L https://artifactory.prd.it.unity3d.com/artifactory/api/gpg/key/public | sudo apt-key add -
      -  sudo sh -c "echo 'deb https://artifactory.prd.it.unity3d.com/artifactory/unity-apt-local bionic main' > /etc/apt/sources.list.d/unity.list"
      -  sudo apt-get update
      -  sudo apt-get install yamato-parser -y
      -  |5-

                     if [[ "$GIT_BRANCH" != "yamato/old-package-ci-image-ci" ]]; then
                         echo "Should run on 'yamato/old-package-ci-image-ci' but is running on '$GIT_BRANCH'"
                         exit 1
                     fi
      -  git config --global user.name "noreply@unity3d.com"
      -  git config --global user.email "noreply@unity3d.com"
      -  pipenv run python3 .yamato/script/editor_scripts/merge_revisions.py --revision $GIT_REVISION --target-branch yamato/old-package-ci-image --track trunk
editor-pinning-merge-all:
    name: Merge all [no CI][trunk no ABV]
    variables:
        CI: true
    dependencies:
      -  .yamato/_editor_pinning.yml#editor-pinning-merge-revisions-trunk
editor-pinning-merge-all-ci:
    name: Merge all [CI][trunk no ABV]
    triggers:
        expression: push.branch eq "yamato/old-package-ci-image-ci" AND push.changes.any match "**/_latest_editor_versions*.metafile"
    variables:
        CI: true
    dependencies:
      -  .yamato/_editor_pinning.yml#editor-pinning-merge-revisions-trunk
editor-pinning-store-green:
    name: Store green job revisions
    agent:
        type: Unity::VM
        image: package-ci/ubuntu:v1.2.4-701242
        flavor: b1.small
    commands:
      -  sudo pip3 install pipenv --index-url https://artifactory.prd.it.unity3d.com/artifactory/api/pypi/pypi/simple
      -  python3 -m pipenv install --dev
      -  git config --global user.name "noreply@unity3d.com"
      -  git config --global user.email "noreply@unity3d.com"
      -  pipenv run python3 .yamato/script/editor_scripts/store_green_revisions.py --target-branch $GIT_BRANCH --track trunk --apikey $YAMATO_KEY
    triggers:
        recurring:
          -  branch: yamato/old-package-ci-image
             frequency: 7 * * ?
