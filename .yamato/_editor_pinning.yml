 
# This file is generated by .yamato/ruamel/build.py. Don't edit this file manually. 
# Introduce any changes under .yamato/config/*.metafile files (for most cases) or under .yamato/ruamel/* within Python (more elaborate cases), and rerun build.py to regenerate all .yml files. 
# Read more under .yamato/docs/readme.md 

{% metadata_file .yamato/_latest_editor_versions_2020.2.metafile -%}

---

editor-pinning-target-to-ci:
    name: Merge 10.x.x/release to 10.x.x/ci/editor-pinning
    agent:
        type: Unity::VM
        image: package-ci/ubuntu:stable
        flavor: b1.small
    variables:
        CI: true
    commands:
      -  echo $GIT_BRANCH
      -  |5-

                     if [[ "$GIT_BRANCH" != "10.x.x/release" ]]; then
                         echo "Should run on '10.x.x/release' but is running on '$GIT_BRANCH'"
                         exit 1
                     fi
      -  git fetch
      -  git checkout 10.x.x/release
      -  git checkout 10.x.x/ci/editor-pinning
      -  git config --global user.name "noreply@unity3d.com"
      -  git config --global user.email "noreply@unity3d.com"
      -  git merge 10.x.x/release --no-ff -X theirs
      -  git push
editor-pinning-update:
    name: Update pinned editor versions
    agent:
        type: Unity::VM
        image: package-ci/ubuntu:stable
        flavor: b1.small
    variables:
        CI: true
    commands:
      -  sudo pip3 install pipenv --index-url https://artifactory.prd.it.unity3d.com/artifactory/api/pypi/pypi/simple
      -  python3 -m pipenv install --dev
      -  curl -L https://artifactory.prd.it.unity3d.com/artifactory/api/gpg/key/public | sudo apt-key add -
      -  sudo sh -c "echo 'deb https://artifactory.prd.it.unity3d.com/artifactory/unity-apt-local bionic main' > /etc/apt/sources.list.d/unity.list"
      -  sudo apt-get update
      -  sudo apt-get install yamato-parser -y
      -  |5-

                     if [[ "$GIT_BRANCH" != "10.x.x/release" ]]; then
                         echo "Should run on '10.x.x/release' but is running on '$GIT_BRANCH'"
                         exit 1
                     fi
      -  git config --global user.name "noreply@unity3d.com"
      -  git config --global user.email "noreply@unity3d.com"
      -  pipenv run python3 .yamato/ruamel/editor_pinning/update_revisions.py --target-branch 10.x.x/ci/editor-pinning --force-push
    dependencies:
      -  .yamato/_editor_pinning.yml#editor-pinning-target-to-ci
    triggers:
        recurring:
          -  branch: 10.x.x/release
             frequency: 23 * * ?
editor-pinning-merge-revisions-2020.2:
    name: Merge [2020.2] revisions to 10.x.x/release [no ABV]
    agent:
        type: Unity::VM
        image: package-ci/ubuntu:stable
        flavor: b1.small
    variables:
        CI: true
    commands:
      -  sudo pip3 install pipenv --index-url https://artifactory.prd.it.unity3d.com/artifactory/api/pypi/pypi/simple
      -  python3 -m pipenv install --dev
      -  curl -L https://artifactory.prd.it.unity3d.com/artifactory/api/gpg/key/public | sudo apt-key add -
      -  sudo sh -c "echo 'deb https://artifactory.prd.it.unity3d.com/artifactory/unity-apt-local bionic main' > /etc/apt/sources.list.d/unity.list"
      -  sudo apt-get update
      -  sudo apt-get install yamato-parser -y
      -  |5-

                     if [[ "$GIT_BRANCH" != "10.x.x/ci/editor-pinning" ]]; then
                         echo "Should run on '10.x.x/ci/editor-pinning' but is running on '$GIT_BRANCH'"
                         exit 1
                     fi
      -  git config --global user.name "noreply@unity3d.com"
      -  git config --global user.email "noreply@unity3d.com"
      -  pipenv run python3 .yamato/ruamel/editor_pinning/merge_revisions.py --revision $GIT_REVISION --target-branch 10.x.x/release --track 2020.2
editor-pinning-merge-revisions-2020.2-abv:
    name: Merge [2020.2] revisions to 10.x.x/release [ABV]
    allow_failure: true
    dependencies:
      -  .yamato/_abv.yml#all_project_ci_2020.2
    agent:
        type: Unity::VM
        image: package-ci/ubuntu:stable
        flavor: b1.small
    variables:
        CI: true
    commands:
      -  sudo pip3 install pipenv --index-url https://artifactory.prd.it.unity3d.com/artifactory/api/pypi/pypi/simple
      -  python3 -m pipenv install --dev
      -  curl -L https://artifactory.prd.it.unity3d.com/artifactory/api/gpg/key/public | sudo apt-key add -
      -  sudo sh -c "echo 'deb https://artifactory.prd.it.unity3d.com/artifactory/unity-apt-local bionic main' > /etc/apt/sources.list.d/unity.list"
      -  sudo apt-get update
      -  sudo apt-get install yamato-parser -y
      -  |5-

                     if [[ "$GIT_BRANCH" != "10.x.x/ci/editor-pinning" ]]; then
                         echo "Should run on '10.x.x/ci/editor-pinning' but is running on '$GIT_BRANCH'"
                         exit 1
                     fi
      -  git config --global user.name "noreply@unity3d.com"
      -  git config --global user.email "noreply@unity3d.com"
      -  pipenv run python3 .yamato/ruamel/editor_pinning/merge_revisions.py --revision $GIT_REVISION --target-branch 10.x.x/release --track 2020.2
editor-pinning-merge-all:
    name: Merge all [no CI][2020.2 no ABV]
    variables:
        CI: true
    dependencies:
      -  .yamato/_editor_pinning.yml#editor-pinning-merge-revisions-2020.2
editor-pinning-merge-all-ci:
    name: Merge all [CI][2020.2 no ABV]
    triggers:
        expression: push.branch eq "10.x.x/ci/editor-pinning" AND push.changes.any match "**/_latest_editor_versions*.metafile"
    variables:
        CI: true
    dependencies:
      -  .yamato/_editor_pinning.yml#editor-pinning-merge-revisions-2020.2
editor-pinning-store-green:
    name: Store green job revisions
    agent:
        type: Unity::VM
        image: package-ci/ubuntu:stable
        flavor: b1.small
    commands:
      -  sudo pip3 install pipenv --index-url https://artifactory.prd.it.unity3d.com/artifactory/api/pypi/pypi/simple
      -  python3 -m pipenv install --dev
      -  git config --global user.name "noreply@unity3d.com"
      -  git config --global user.email "noreply@unity3d.com"
      -  pipenv run python3 .yamato/ruamel/editor_pinning/store_green_revisions.py --target-branch $GIT_BRANCH --track 2020.2 --apikey $YAMATO_KEY
    triggers:
        recurring:
          -  branch: 10.x.x/release
             frequency: 7 * * ?
