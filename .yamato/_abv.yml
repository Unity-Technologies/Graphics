
# This file is generated by ant_yaml_generation package, don't edit this file manually.
# Introduce any changes under .yamato/config/*.metafile files and rerun yaml-generation (or check #devs-automation-tooling).

{% metadata_file .yamato/_cache_config.metafile -%}
{% metadata_file .yamato/_latest_editor_versions_2022.3.metafile -%}

---

graphics_repo_nightly_2022.3:
    name: Pack & Test all templates on 2022.3
    variables:
        CACHE_ARGS: '{{cache.flags}}'
        CUSTOM_REVISION: '{{2022_3.changeset.id}}'
        TEST_FILTER: .*
        UTR_VERSION: "current"
    triggers:
        recurring:
          -  branch: 2022.3/staging
             frequency: weekly
    dependencies:
      -  path: .yamato/_templates.yml#all_template_ci_2022.3
         rerun: on_new_revision
graphics_repo_nightly_CUSTOM-REVISION:
    name: Pack & Test all templates on CUSTOM-REVISION
    variables:
        CACHE_ARGS: '{{cache.flags}}'
        CUSTOM_REVISION: custom_revision_not_set
        TEST_FILTER: .*
        UTR_VERSION: "current"
    dependencies:
      -  path: .yamato/_templates.yml#all_template_ci_CUSTOM-REVISION
         rerun: on_new_revision
