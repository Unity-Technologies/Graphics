default:
  area: SRP Foundation

workflow:
  when:
    path:
      - ^.*Volume.*$
  area: SRP Settings

tools:
  when:
    path:
      - ^.*DebugManager.*$
  area: Graphics Tools

xr:
  when:
    path:
      - ^.*XRSubsystem.*$
  area: SRP XR
