default:
  area: SRP Foundation

settings:
  when:
    path:
      - ^.*Volume.*$
  area: SRP Settings

debugging-tools:
  when:
    path:
      - ^.*DebugManager.*$
  area: Graphics Debugging Tools

xr:
  when:
    path:
      - ^.*XRSubsystem.*$
  area: Graphics XR
