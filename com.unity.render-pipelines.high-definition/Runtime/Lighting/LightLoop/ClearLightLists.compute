#pragma kernel ClearList

RWStructuredBuffer<uint>  _LightListToClear;
int _LightListEntries;
int _Offset;

[numthreads(64, 1, 1)]
void ClearList(uint3 id : SV_DispatchThreadID)
{
    int x = id.x + _Offset;
    if (x < (uint)_LightListEntries)
        _LightListToClear[x] = 0;
}
